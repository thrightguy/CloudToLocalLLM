# Stage 1: Build the Flutter web app
FROM ghcr.io/cirruslabs/flutter:latest AS build

# Create a non-root user
RUN groupadd -r flutteruser && useradd --no-log-init -r -g flutteruser -m -s /bin/bash flutteruser

# As root, make flutteruser own the SDK. This is crucial.
USER root
RUN chown -R flutteruser:flutteruser /sdks/flutter

# Switch to flutteruser for all subsequent app-related operations
USER flutteruser
WORKDIR /app

# Copy application code (now as flutteruser, into flutteruser's WORKDIR /app)
COPY . .

# Mark Flutter SDK as safe (as flutteruser, who now owns it)
RUN git config --global --add safe.directory /sdks/flutter

# Run the build
RUN flutter pub get && flutter build web --release

# Stage 2: Serve the app with Nginx
FROM nginx:alpine
COPY --from=build /app/build/web /usr/share/nginx/html
COPY config/nginx/nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"] 